# P13-T03 Lane Handoff

* Scope: `STC/Control/Rules.lean`, `STC/Examples/GlobalRules.lean`.
* Result: one typed orchestration/lifecycle rule family and derived subfamily
  views; `Step`/`Trace` reuse is through `globalControlModel`.
* Evidence: `A I E`; concrete fixture labels are inhabited, while guard/WF
  derivation remains a profile-dependent follow-up.
